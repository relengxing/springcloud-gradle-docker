app:
  environment: default

server:
    port: 7001
logging:
  file: /tmp/client-log.log
  level: info
spring:
  application:
    name: dm-service
eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:6001/eureka/
