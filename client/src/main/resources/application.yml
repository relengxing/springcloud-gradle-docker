app:
  environment: default

server:
    port: 7100
logging:
  file: /tmp/client-log.log
  level: info
spring:
  application:
    name: dm-service
eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:7000/eureka/
